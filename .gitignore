# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Gerado em build/dev a partir do index.html (scripts/gen-at-sp.mjs) — nunca editar à mão
/assistente-tecnico-medico-sp.html

# Idem para a porta nacional (scripts/gen-at-br.mjs) — nunca editar à mão
/assistente-tecnico-medico-br.html

# Local-only: força o npm a usar PowerShell por causa do "&" no caminho da pasta (Windows).
# NÃO commitar — quebraria o build Linux na Vercel/Netlify (powershell.exe não existe lá).
.npmrc

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
